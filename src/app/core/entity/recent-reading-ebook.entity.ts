export interface RecentReadingBook {
  bookId: string,
  bookCoverUrl?: string,
  bookTitle?: string,
  bookAuthors?: string,
  bookFileUrl?: string,
  latestReadTime?: string,
  progressPercentage?: string,
  progressTitle?: string,
  progressLocationIndex?: string
}
