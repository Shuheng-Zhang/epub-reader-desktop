export interface GeneralBook {
    id?: string,
    bookTitle?: string,
    bookAuthors?: string,
    bookDescription?: string,
    bookCoverUrl?: string,
    bookSize?: string,
    bookPushedTime?: string
    bookFileUrl?: string
}